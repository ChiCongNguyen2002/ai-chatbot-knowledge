version: '3.8'

services:
  chatbot:
    build:
      context: .
      dockerfile: Dockerfile.phase2
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: python app_phase2.py
